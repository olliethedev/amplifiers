type Todo @model {
  content: String!
}


# type Query {
#   fetchTodos: [Todo] @testHttp(url: "https://www.example.com/posts")
# }

type Mutation {
  sendEmailCampaign( emails: [String!]! ): Boolean! @fooFunction(name: "sup")
  # test(value: String): Boolean! @function(name: "test")
}

